# build

- use template in `templates` folder
- create pages using python file in `pages` folder
- generate pages

```commandline
pypy generate.py
```

# develop

use `serve.py` for hot reload

```commandline
pypy serve.py
```

# deploy

all necessary files should be in `public` folder

```commandline
pypy serve.py
```
